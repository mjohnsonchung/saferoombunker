{
  "name": "saferoombunker-website",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "resend": "^4.0.0"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  },
  "scripts": {
    "build:css": "tailwindcss -i tailwind.input.css -o styles.css --minify",
    "build": "node build.cjs"
  }
}
